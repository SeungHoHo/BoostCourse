<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.seungho.android.myapplication.movieapp">

    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/main"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:usesCleartextTraffic="true"
        android:theme="@style/Theme.MovieApp">
        <activity
            android:name=".View.Activity.ViewPagerActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".View.Activity.ReviewList.ReviewListAActivity" />
        <activity android:name=".View.Activity.ReviewList.ReviewListBActivity"/>
        <activity android:name=".View.Activity.ReviewList.ReviewListCActivity"/>
        <activity android:name=".View.Activity.ReviewList.ReviewListDActivity"/>
        <activity android:name=".View.Activity.ReviewList.ReviewListEActivity"/>
        <activity android:name=".View.Activity.MatrixActivity"/>
    </application>

</manifest>